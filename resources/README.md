# Extra resources

Optional demos and non-production tooling bundled with the repo.

- **`streamlit_recruitment/`** — standalone Streamlit UI for experimentation (Python). Not used by the Vite + Express deployment path.
