# Optional: set when the API is not served from the same origin as the UI.
# Local default is empty string → same-origin `/api` (Vite dev proxy → localhost:4000).
# VITE_API_BASE_URL=

# Set to "false" on Windows if you prefer to start the backend manually (`npm run dev:backend`).
# VITE_SPAWN_BACKEND=true
